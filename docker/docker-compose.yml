services:
  mysql:
    image: mysql:8.4
    restart: always
    container_name: mysql-eventlinker
    environment:
      - MYSQL_ROOT_PASSWORD=mysql
    ports: 
      - '3336:3306'
    networks: 
      - eventlinker-network
networks:
  eventlinker-network:
    driver: bridge